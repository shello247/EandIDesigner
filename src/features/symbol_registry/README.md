# Symbol Registry

Owns approved SVG symbols, draft versions, validation results, and approval
state. Other features should use registry actions/services instead of writing
symbol records directly.

## Test

```powershell
npm run test -- symbol_registry
```

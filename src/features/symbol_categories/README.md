# Symbol Categories

Symbol categories provide one global, user-managed organizational taxonomy for
the Symbol Registry and drawing symbol library.

Categories do not control technical behavior. Network, terminal-block, cable,
tag, scale, and placement behavior continues to use the symbol registry's
technical kind and explicit metadata capabilities.

Every symbol is assigned one current category across all of its versions.
`Other` is the protected fallback category.

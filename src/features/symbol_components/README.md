# Symbol Components

Optional Figma-authored component positions for registry symbols. Definitions are
stored in versioned symbol metadata, while drawing assets pin recursive component
selections to exact child symbol versions. Component artwork is composed only on
panel-layout occurrences.

Run the focused tests with:

```powershell
npm run test -- src/features/symbol_components/tests
```
